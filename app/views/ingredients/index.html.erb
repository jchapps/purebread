<h1>Ingredients</h1>
<%  @ingredients.each do |ingredient| %>
  <ul>
    <%= ingredient.name %>
    <%= ingredient.description %>
    <div>
      <%=link_to "Show Sandwiches #{ingredient.name} is in!", ingredient_path(ingredient) %>
    </div>
  </ul>
<% end %>
